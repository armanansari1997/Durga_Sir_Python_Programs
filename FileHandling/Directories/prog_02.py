# To create a subdirectory in the current working directory
import os

os.mkdir('valmiki123')
print('valmiki123 is created in cwd')

# Note : If the specified directory is already exist then we will get FileExistError
